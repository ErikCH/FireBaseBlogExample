import { excerpt } from '../../../helpers/excerpt';
import { module, test } from 'qunit';

module('Unit | Helper | excerpt');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = excerpt(42);
  assert.ok(result);
});
